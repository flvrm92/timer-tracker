<?xml version="1.0" encoding="utf-8"?>
<!--
  MSIX manifest template for Timer Tracker.

  Do not edit the generated packaging/Package.appxmanifest - it is rewritten on
  every build by forge.config.js and is gitignored. Edit this template, or
  packaging/identity.json for the identity fields.

  Placeholders substituted at build time:
    {{IDENTITY_NAME}}          packaging/identity.json -> identityName
    {{PUBLISHER}}              packaging/identity.json -> publisher
    {{PUBLISHER_DISPLAY_NAME}} packaging/identity.json -> publisherDisplayName
    {{DISPLAY_NAME}}           packaging/identity.json -> displayName
    {{DESCRIPTION}}            packaging/identity.json -> description
    {{VERSION}}                package.json version, widened to four parts
-->
<Package
  xmlns="http://schemas.microsoft.com/appx/manifest/foundation/windows10"
  xmlns:uap="http://schemas.microsoft.com/appx/manifest/uap/windows10"
  xmlns:rescap="http://schemas.microsoft.com/appx/manifest/foundation/windows10/restrictedcapabilities"
  IgnorableNamespaces="uap rescap">

  <Identity Name="{{IDENTITY_NAME}}"
            Publisher="{{PUBLISHER}}"
            Version="{{VERSION}}"
            ProcessorArchitecture="x64" />

  <Properties>
    <DisplayName>{{DISPLAY_NAME}}</DisplayName>
    <PublisherDisplayName>{{PUBLISHER_DISPLAY_NAME}}</PublisherDisplayName>
    <Logo>assets\StoreLogo.png</Logo>
    <Description>{{DESCRIPTION}}</Description>
  </Properties>

  <Dependencies>
    <!-- 10.0.17763.0 is Windows 10 1809, the oldest build still worth supporting. -->
    <TargetDeviceFamily Name="Windows.Desktop"
                        MinVersion="10.0.17763.0"
                        MaxVersionTested="10.0.26100.0" />
  </Dependencies>

  <Resources>
    <Resource Language="en-us" />
  </Resources>

  <Applications>
    <!--
      The Forge MSIX maker nests the packaged Electron app under an `app\`
      folder inside the package, so the `app\` prefix here is mandatory. (The
      winapp-CLI packaging route does NOT nest, and would omit it.)
    -->
    <Application Id="TimerTracker"
                 Executable="app\time-tracker.exe"
                 EntryPoint="Windows.FullTrustApplication">
      <uap:VisualElements
        DisplayName="{{DISPLAY_NAME}}"
        Description="{{DESCRIPTION}}"
        BackgroundColor="#2563eb"
        Square150x150Logo="assets\Square150x150Logo.png"
        Square44x44Logo="assets\Square44x44Logo.png">
        <uap:DefaultTile
          Wide310x150Logo="assets\Wide310x150Logo.png"
          Square310x310Logo="assets\Square310x310Logo.png"
          Square71x71Logo="assets\Square71x71Logo.png" />
        <uap:SplashScreen Image="assets\SplashScreen.png" />
      </uap:VisualElements>
    </Application>
  </Applications>

  <Capabilities>
    <!--
      Standard declaration for a Win32/Electron desktop app. It is a restricted
      capability, so Partner Center asks for a one-line justification at
      submission, but it is auto-approved for full-trust desktop apps.
    -->
    <rescap:Capability Name="runFullTrust" />
  </Capabilities>
</Package>
